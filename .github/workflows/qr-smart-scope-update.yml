name: QR scope test
on:
  push:
    branches:
      - feat/qr-smart-scope-20260813-v2
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "QR workflow is running"
